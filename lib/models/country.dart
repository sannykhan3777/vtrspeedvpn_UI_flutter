class CountryModel {
  String location; // location name for UI
  String flag; // url to an asset flag icon
  String url; // location url for api endpoint

  CountryModel({required this.location, required this.flag, required this.url});
}